<Window x:Class="Schedule.Events.PromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Schedule.Events"
        mc:Ignorable="d"
        Title="Input Event count" Height="251.396" Width="352.514">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="73*"/>
            <ColumnDefinition Width="272*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Text="Enter events count" Margin="10,32,10,161" Grid.ColumnSpan="2" />
        <TextBox x:Name="EventCountTextBox" Margin="10,87,10,90" Grid.ColumnSpan="2" PreviewTextInput="NumberValidationTextBox"/>
        <Button Content="OK" Click="OKButton_Click" Margin="10,156,190,10" Grid.ColumnSpan="2" />
        <Button Content="Cancel" Click="CancelButton_Click" Margin="117.324,156,10,10" Grid.Column="1" />
    </Grid>
</Window>
